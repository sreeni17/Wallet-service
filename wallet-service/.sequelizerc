const path = require('path');
module.exports = {
    "env": process.env.NODE_ENV || 'development',
  "config": path.resolve('./sequelize/index.js'),
  "models-path": path.resolve('./sequelize/models'),
  "migrations-path": path.resolve('./sequelize/migrations')
};
